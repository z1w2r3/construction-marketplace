---
name: professional-review
description: "专业化审校技能 - 对生成的报告进行三遍审校,去除AI味,提升专业度。包含内容审校、风格审校、细节打磨"
---

# Professional Review Skill
专业化审校技能 - 三遍审校机制

## 技能说明
对生成的报告/文档进行系统化审校,确保内容准确、风格专业、细节完善。

**核心价值** (来自文章启发):
- ✅ 去AI味:删除套话、替换书面词汇、拆解AI句式
- ✅ 提升可信度:强化来源标注、避免编造
- ✅ 增强可读性:优化句子长度、段落结构、节奏感

---

## 输入参数

- `content`: 待审校的内容(Markdown格式)
- `document_type`: 文档类型
  - `report` - 报告(默认)
  - `summary` - 摘要
  - `analysis` - 分析文档
- `strictness`: 审校严格度
  - `strict` - 严格(三遍全部执行)
  - `moderate` - 中等(执行第一和第二遍)
  - `light` - 轻度(只执行第一遍)
- `output_changes`: 是否输出修改清单(默认 true)

---

## 执行逻辑

### 第一遍: 内容审校(事实、逻辑、结构)

#### 检查项 1: 事实准确性

扫描所有事实性陈述,检查是否有来源标注:

```python
# 识别模式
fact_patterns = [
    r'项目.*于.*\d{4}年',  # 时间相关
    r'预算.*\d+万',        # 数字相关
    r'采用.*架构',         # 技术相关
    r'完成.*\d+%',         # 百分比相关
]

for sentence in sentences:
    if matches_fact_pattern(sentence):
        # 检查是否有来源标注
        has_source = check_source_citation(sentence)
        if not has_source:
            flag_issue("缺少来源标注", sentence)
```

**标注格式识别**:
- ✅ "根据《XX文档》(第N页)..."
- ✅ "来源: 《XX文档》"
- ✅ "[1] 《XX文档》"
- ❌ "项目于2024年启动"(无来源)

**修正示例**:
```
❌ 原文: "项目采用微服务架构"
✅ 修正: "根据《技术方案》(第5页),项目采用微服务架构"

⚠️  如果无法找到来源:
标注: "[需要补充来源] 项目采用微服务架构"
```

---

#### 检查项 2: 逻辑一致性

检查前后陈述是否矛盾:

```python
contradictions = []

# 例子:
sentence1 = "项目于2024年1月启动"
sentence2 = "项目运行已超过两年"  # 矛盾(当前2025年10月)

if detect_contradiction(sentence1, sentence2):
    contradictions.append({
        "type": "时间矛盾",
        "location": [sentence1_位置, sentence2_位置],
        "suggestion": "请核实项目启动时间"
    })
```

**常见矛盾类型**:
- 时间矛盾
- 数据矛盾(总数 vs 分项数据)
- 观点矛盾(前面说优点,后面说缺点但没有说明转折)

---

#### 检查项 3: 结构完整性

检查报告结构是否完整:

```python
required_sections = {
    "report": ["概述", "主体内容", "结论"],
    "summary": ["关键要点", "下一步"],
    "analysis": ["问题", "分析", "建议"]
}

missing = check_missing_sections(content, document_type)
if missing:
    flag_issue("缺少必要章节", missing)
```

---

#### 检查项 4: 禁止编造

扫描可能的编造内容:

```python
# 高风险词汇(通常表示编造)
risky_phrases = [
    "约为", "大约", "估计", "可能",
    "一般来说", "通常", "往往"
]

# 如果这些词汇出现,但没有明确的文档来源,标记
for phrase in risky_phrases:
    if phrase in sentence and not has_source_citation(sentence):
        flag_warning("可能存在推测性表述,需要标注", sentence)
```

**修正示例**:
```
❌ 原文: "项目预算约为500万"
✅ 修正: "根据《预算表》,项目预算为500万" (如果有来源)
✅ 修正: "[文档库中未找到预算信息]" (如果没有来源)
```

---

### 第一遍审校输出

```markdown
## 📋 第一遍审校 - 内容检查

### ✅ 通过项
- 逻辑一致性: 无矛盾
- 结构完整性: 包含所有必要章节

### ⚠️  发现问题 (3项)

**问题 1: 缺少来源标注**
- 位置: 第2页,第3段
- 原文: "项目采用微服务架构"
- 建议: 补充来源,如"根据《技术方案》(第5页)..."

**问题 2: 可能存在推测**
- 位置: 第5页,第2段
- 原文: "预算约为500万"
- 建议: 明确数据来源或标注"文档库中未找到"

**问题 3: 数据矛盾**
- 位置: 第3页和第7页
- 矛盾: "完成80%"(第3页) vs "完成75%"(第7页)
- 建议: 核实准确数据

### 📊 统计
- 检查句子数: 156
- 发现问题: 3
- 通过率: 98%
```

---

---

### 第二遍: 风格审校(去AI味)

#### 任务 1: 删除AI套话

扫描并删除常见AI套话:

```python
ai_cliches = {
    "在当今时代": "",
    "综上所述": "",
    "值得注意的是": "",
    "充分利用": "使用",
    "显著提升": "",  # 需要替换为具体数字
    "不断优化": "优化",
    "持续改进": "改进",
    "进一步加强": "加强",
    "切实有效": "有效",
}

for cliche, replacement in ai_cliches.items():
    content = content.replace(cliche, replacement)
```

**删除示例**:
```
❌ 原文: "在当今时代,微服务架构已成为主流"
✅ 修正: "微服务架构已成为主流"

❌ 原文: "综上所述,项目进展顺利"
✅ 修正: "项目进展顺利"
```

---

#### 任务 2: 替换书面词汇

将书面化表达替换为具体/口语化表达:

```python
replacements = {
    "显著提升": lambda ctx: extract_specific_number(ctx),  # 提取具体数字
    "充分利用": "用好",
    "进行操作": lambda ctx: extract_verb(ctx),  # 提取具体动词
    "相关数据显示": lambda ctx: f"根据{extract_source(ctx)},数据显示",
    "深入分析": "分析",
    "全面了解": "了解",
}
```

**替换示例**:
```
❌ 原文: "性能显著提升"
✅ 修正: "性能提升30%(来源:《测试报告》)"

❌ 原文: "充分利用Redis的特性"
✅ 修正: "用好Redis的特性"

❌ 原文: "进行代码重构操作"
✅ 修正: "重构代码"
```

---

#### 任务 3: 拆解AI句式

识别并拆解典型的AI句式:

```python
# Pattern 1: "不是...而是..."连续出现
if count_pattern(content, "不是.*而是") > 2:
    suggest_rewrite("避免重复使用'不是...而是...',换用其他表达")

# Pattern 2: "首先...其次...最后..."过于机械
if has_rigid_structure(content, ["首先", "其次", "再次", "最后"]):
    suggest_rewrite("使用更自然的过渡词,如'另外'、'此外'、'同时'")

# Pattern 3: 长句嵌套
for sentence in sentences:
    if count_commas(sentence) > 3:  # 超过3个逗号
        suggest_split(sentence, "建议拆分为多个短句")
```

**拆解示例**:
```
❌ 原文:
"项目在技术架构方面采用了微服务架构,并且在数据库选型上选择了Redis,同时在消息队列方面使用了Kafka,这些都经过充分调研。"

✅ 修正:
"技术架构方面,项目采用微服务架构。
具体选型:
- 数据库缓存: Redis
- 消息队列: Kafka

所有选型均经过充分调研(《技术讨论纪要》2024-02-10)。"
```

---

#### 任务 4: 加入具体细节

将抽象表述替换为具体细节:

```python
# 识别抽象表述
abstract_phrases = [
    "进展顺利",
    "表现良好",
    "成效显著",
    "问题较多",
]

for phrase in abstract_phrases:
    if phrase in content and not followed_by_details(phrase):
        suggest_add_details(phrase, "补充具体数据或案例")
```

**加入细节示例**:
```
❌ 原文: "项目进展顺利"
✅ 修正: "截至2024年9月,已完成80%的开发任务,测试覆盖率达到85%(来源:《月报-2024-09》)"

❌ 原文: "性能表现良好"
✅ 修正: "性能测试结果: QPS达到10000,平均响应时间50ms(来源:《性能测试报告》)"
```

---

### 第二遍审校输出

```markdown
## 🎨 第二遍审校 - 风格优化

### 删除的AI套话 (8处)
- "在当今时代" x 2
- "综上所述" x 1
- "值得注意的是" x 3
- "显著提升" x 2

### 替换的书面词汇 (12处)
- "充分利用" → "用好" x 4
- "进行操作" → 具体动词 x 3
- "显著提升" → 具体数字 x 5

### 拆解的长句 (5处)
- 第3页第2段: 拆分为3个短句
- 第5页第1段: 拆分为2个短句
- ...

### 补充的具体细节 (6处)
- "进展顺利" → 补充完成百分比和时间
- "性能良好" → 补充QPS和响应时间
- ...

### 📊 统计
- AI味词汇减少: 20处
- 具体细节增加: 6处
- 平均句子长度: 从28字降低到22字
```

---

---

### 第三遍: 细节打磨(节奏、排版)

#### 检查 1: 句子长度

统计每个句子的长度,标记过长的句子:

```python
for sentence in sentences:
    length = len(sentence)

    if length > 30:
        flag_issue("句子过长", sentence, f"建议缩短到30字以内(当前{length}字)")
    elif length < 5:
        flag_warning("句子过短", sentence, "考虑合并或扩展")
```

**理想分布**:
- 短句(< 15字): 20%
- 中等句(15-25字): 60%
- 长句(25-30字): 20%
- 超长句(> 30字): 0%

**优化示例**:
```
❌ 原文(45字): "根据技术方案文档显示,项目在架构设计方面采用了微服务架构,同时在数据持久化层面选择了Redis作为缓存方案,消息传递使用Kafka。"

✅ 修正:
"技术架构采用微服务设计(《技术方案》第5页)。
数据层选型:
- 缓存: Redis
- 消息队列: Kafka"
```

---

#### 检查 2: 段落长度

统计段落行数,标记过长的段落:

```python
for paragraph in paragraphs:
    lines = count_lines(paragraph)  # 假设手机屏幕宽度

    if lines > 5:
        flag_issue("段落过长", paragraph, f"建议拆分(当前{lines}行)")
```

**理想长度**: 手机屏幕3-5行

**优化方法**:
- 长段落拆分为多个小段
- 使用列表代替长段落
- 添加小标题分隔

---

#### 检查 3: 标点使用

检查标点使用是否自然:

```python
# 检查逗号过多
for sentence in sentences:
    comma_count = sentence.count('，')

    if comma_count > 2:
        suggest("使用句号分割,而非逗号连接", sentence)

# 检查顿号使用
if '、' in content:
    suggest("考虑使用列表代替顿号", "提高可读性")
```

**优化示例**:
```
❌ 原文: "项目采用Redis、Kafka、MySQL、MongoDB等技术。"
✅ 修正:
"项目采用的技术:
- Redis (缓存)
- Kafka (消息队列)
- MySQL (关系数据库)
- MongoDB (文档数据库)"
```

---

#### 检查 4: 节奏变化

检查句子长度是否有变化,避免单调:

```python
sentence_lengths = [len(s) for s in sentences]

# 检查是否缺乏节奏变化
variance = calculate_variance(sentence_lengths)
if variance < 10:  # 方差太小,说明长度太一致
    suggest("增加句子长度变化", "长短句交替使用")

# 检查是否有"快慢结合"
fast_sentences = [s for s in sentences if len(s) < 15]  # 快
slow_sentences = [s for s in sentences if len(s) > 25]  # 慢

ratio = len(fast_sentences) / len(slow_sentences)
if ratio < 0.5 or ratio > 2:
    suggest("调整快慢句比例", "建议接近1:1")
```

---

#### 检查 5: 专业术语解释

检查专业术语首次出现时是否有解释:

```python
technical_terms = extract_technical_terms(content)  # 识别专业术语

for term in technical_terms:
    first_occurrence = find_first_occurrence(term, content)

    if not has_explanation(first_occurrence):
        suggest(f"首次出现'{term}'时建议添加简短解释", first_occurrence)
```

**示例**:
```
❌ 原文: "项目采用CQRS模式..."
✅ 修正: "项目采用CQRS模式(命令查询职责分离)..."
```

---

### 第三遍审校输出

```markdown
## ✨ 第三遍审校 - 细节打磨

### 句子长度优化 (7处)
- 拆分超长句: 5处
- 合并过短句: 2处
- 优化后平均长度: 22字(之前28字)

### 段落结构优化 (4处)
- 拆分长段落: 3处
- 添加小标题: 1处

### 标点优化 (9处)
- 逗号改句号: 5处
- 顿号改列表: 4处

### 节奏调整
- 快句(< 15字): 25% (优化前 15%)
- 中等句(15-25字): 55% (优化前 70%)
- 长句(25-30字): 20% (优化前 15%)

### 术语解释 (3处)
- "CQRS" → 添加解释
- "DDD" → 添加解释
- "K8s" → 添加解释

### 📊 可读性提升
- Flesch阅读轻松度: 68 → 75 (提升7分)
- 平均句子长度: 28字 → 22字
- 段落平均行数: 6行 → 4行
```

---

---

## 汇总输出

### 最终审校报告

```markdown
# 📝 审校完成报告

## 审校概览
- 审校模式: 严格(三遍全部执行)
- 原文长度: 3520字
- 修改后长度: 3680字
- 总修改项: 31处

---

## 第一遍: 内容审校
- ✅ 通过项: 逻辑一致性、结构完整性
- ⚠️  发现问题: 3处
  - 缺少来源标注: 1处 → 已修正
  - 可能存在推测: 1处 → 已标注
  - 数据矛盾: 1处 → 已核实修正

---

## 第二遍: 风格审校
- 删除AI套话: 8处
- 替换书面词汇: 12处
- 拆解长句: 5处
- 补充具体细节: 6处

---

## 第三遍: 细节打磨
- 句子长度优化: 7处
- 段落结构优化: 4处
- 标点优化: 9处
- 术语解释: 3处

---

## 质量评估
- ✅ 事实准确性: 100% (所有事实已标注来源)
- ✅ AI味检测: 低 (删除20处AI味词汇)
- ✅ 可读性: 75分 (优化前68分)
- ✅ 专业度: 高

---

## 建议
1. 人工复核第1页第3段的数据矛盾修正
2. 考虑补充《预算表》文档以支撑预算相关表述
3. 第5页的技术架构图建议使用实际图片替代文字描述

---

审校完成时间: 2025-10-27 15:30:00
```

---

## 使用示例

### 示例 1: 严格审校模式

```markdown
使用 Skill tool 调用 professional-review:
- content: [报告全文Markdown]
- document_type: "report"
- strictness: "strict"
- output_changes: true

→ 返回: 审校后内容 + 修改清单
```

### 示例 2: 快速审校模式

```markdown
使用 Skill tool 调用 professional-review:
- content: [摘要Markdown]
- document_type: "summary"
- strictness: "light"  # 只执行第一遍
- output_changes: false

→ 返回: 审校后内容(无修改清单)
```

---

## 性能优化

- 批量处理: 一次审校整篇文档
- 增量审校: 只审校修改的部分(TODO)
- 并行处理: 三遍审校可并行执行部分检查(TODO)

---

## 扩展功能 (TODO)

- [ ] 支持自定义AI套话词典
- [ ] 支持特定行业的术语词典
- [ ] 生成审校对比视图(diff)
- [ ] 支持多语言审校(英文等)
